import * as React from 'react';
import type { {{componentName}}State } from './{{componentState}}.types';

export const use{{componentName}}State = (state: {{componentName}}State): {{componentName}}State => {
  const { } = state;

  return state;
}
