import { webLightTheme } from '../theme/lightTheme';
import { themeToTokensObject } from '../themeToTokensObject';

// eslint-disable-next-line no-console
console.log(themeToTokensObject(webLightTheme));
