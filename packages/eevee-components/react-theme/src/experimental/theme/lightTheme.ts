import { createLightTheme } from '../utils/createLightTheme';
import { brandWeb } from '../global';
import type { Theme } from '../theme.types';

export const webLightTheme: Theme = createLightTheme(brandWeb);
