import { IllustrationBlockState } from './IllustrationBlock.types';

export const useIllustrationBlockState = (state: IllustrationBlockState): IllustrationBlockState => {
  return state;
};
/**
 const imageInfos = React.Children.toArray(state.root.children).map(
    (child: any) => child.props
  );
 */
