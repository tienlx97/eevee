/* eslint-disable react-hooks/exhaustive-deps */
import React from 'react';
import Divider from '../Divider';

const DividerDebug = () => {
  return <Divider width={300} />;
};

export default DividerDebug;
