/* eslint-disable react-hooks/exhaustive-deps */
import React from 'react';
import Paragraph from '../Paragraph';

const ParagraphDebug = () => {
  return (
    <Paragraph>
      In CSS, we're given a tool to explicitly control the stacking order of
      HTML elements:
    </Paragraph>
  );
};

export default ParagraphDebug;
