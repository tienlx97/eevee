/* eslint-disable react-hooks/exhaustive-deps */
import React from 'react';
import Blockquote from '../Blockquote';

const BlockquoteDebug = () => {
  return <Blockquote>This is blockquote</Blockquote>;
};

export default BlockquoteDebug;
