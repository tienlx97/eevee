type SpinnerProps = {
  size: number;
  color?: string;
};

type VisuallyHiddenProps = {
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  children: any;
};

export { SpinnerProps, VisuallyHiddenProps };
